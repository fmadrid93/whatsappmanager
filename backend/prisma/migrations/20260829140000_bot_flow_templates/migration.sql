BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[BotFlow] ADD [isTemplate] BIT NOT NULL CONSTRAINT [BotFlow_isTemplate_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH

